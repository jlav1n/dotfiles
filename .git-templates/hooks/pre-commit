#!/bin/bash
# Search for "TODO" in staged files
STAGED_FILES=$(git diff --cached --name-only)

if [ -n "$STAGED_FILES" ]; then
    if echo "$STAGED_FILES" | xargs grep -nH "TODO" 2>/dev/null; then
        echo "❌ Commit aborted: 'TODO' found in staged files."
        exit 1
    fi
fi

exit 0
