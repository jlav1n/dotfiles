defscrollback 10000

caption always "%{kB}%?%-Lw%?%{bw}%n*%f %t%?(%u)%?%{kB}%?%+Lw%?     %c %H %LD, %M %d, %Y"

bindkey "^[[5~" eval "copy" "stuff ^b"  # PgUp | Enter copy mode and page up
bindkey "^[[6~" eval "copy" "stuff ^f"  # PgDown | Enter copy mode and page down
